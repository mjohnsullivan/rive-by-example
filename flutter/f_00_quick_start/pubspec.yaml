name: quick_start
description: Quick start example for a Rive animation in Flutter
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rive: ^0.7.16

flutter:
  uses-material-design: true
